enum BlockType
{
    FUNCTION,
    CLIP,
    START_LOOP,
    END_LOOP,
    CONDITIONAL,
    EFFECT,
    BOOLEAN,
    CALL,
    SPLIT,
    BEAT
    
};

enum TuioAction
{
    ADD,
    REMOVE,
    UPDATE
    
};
